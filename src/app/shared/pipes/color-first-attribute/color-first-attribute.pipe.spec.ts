import { ColorFirstAttributePipe } from './color-first-attribute.pipe';

describe('ColorFirstAttributePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorFirstAttributePipe();
    expect(pipe).toBeTruthy();
  });
});
